/// Placeholder module marker so the package graph builds before M1.
/// Replaced by the real TextBuffer API in milestone M1.
public enum DocumentCoreModule {
    public static let name = "DocumentCore"
}
