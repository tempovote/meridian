name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - name: Install linters (SwiftFormat pinned)
        run: |
          SWIFTFORMAT_VERSION=0.62.1  # keep in sync with README "Tooling"
          curl -fsSL -o /tmp/swiftformat.zip \
            "https://github.com/nicklockwood/SwiftFormat/releases/download/${SWIFTFORMAT_VERSION}/swiftformat.zip"
          unzip -o /tmp/swiftformat.zip -d /tmp/swiftformat
          mkdir -p "$RUNNER_TEMP/pinned-bin"
          install -m 755 "$(find /tmp/swiftformat -name swiftformat -type f | head -1)" "$RUNNER_TEMP/pinned-bin/swiftformat"
          # Runner images ship a pre-installed SwiftFormat under /opt/homebrew/bin,
          # which precedes /usr/local/bin on PATH and silently shadows a pinned
          # install there. Prepend our pinned copy via GITHUB_PATH instead, so it
          # wins for every later step in this job regardless of image contents.
          echo "$RUNNER_TEMP/pinned-bin" >> "$GITHUB_PATH"
          RESOLVED_VERSION="$("$RUNNER_TEMP/pinned-bin/swiftformat" --version)"
          echo "Pinned SwiftFormat version: $RESOLVED_VERSION"
          if [ "$RESOLVED_VERSION" != "$SWIFTFORMAT_VERSION" ]; then
            echo "::error::Downloaded SwiftFormat reports $RESOLVED_VERSION, expected $SWIFTFORMAT_VERSION"
            exit 1
          fi
          brew install swiftlint
      - name: SwiftFormat (lint mode)
        run: swiftformat --lint .
      - name: SwiftLint
        run: swiftlint --strict
      - name: Layer purity
        run: Scripts/lint-imports.sh

  test:
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        package:
          - DocumentCore
          - SyntaxKit
          - SearchKit
          - FileKit
          - ThemeKit
          - SettingsKit
          - EditorUI
          - WorkspaceUI
          - PluginAPI
          - PluginHost
    steps:
      - uses: actions/checkout@v4
      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: Packages/${{ matrix.package }}/.build
          key: spm-${{ runner.os }}-${{ matrix.package }}-${{ hashFiles(format('Packages/{0}/Package.swift', matrix.package), format('Packages/{0}/Package.resolved', matrix.package)) }}
          restore-keys: |
            spm-${{ runner.os }}-${{ matrix.package }}-
      - name: Build (warnings as errors)
        run: swift build --package-path Packages/${{ matrix.package }} -Xswiftc -warnings-as-errors
      - name: Test
        run: swift test --package-path Packages/${{ matrix.package }} -Xswiftc -warnings-as-errors

  test-grammars:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: Grammars/.build
          key: spm-${{ runner.os }}-Grammars-${{ hashFiles('Grammars/Package.swift', 'Grammars/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-Grammars-
      - name: Build (warnings as errors)
        run: swift build --package-path Grammars -Xswiftc -warnings-as-errors
      - name: Test
        run: swift test --package-path Grammars -Xswiftc -warnings-as-errors

  app:
    runs-on: macos-15
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install XcodeGen
        run: brew install xcodegen
      - name: Generate project
        run: xcodegen generate
      - name: Build app (debug, unsigned)
        run: >
          xcodebuild build -project Meridian.xcodeproj -scheme Meridian
          -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO
      - name: UI smoke tests
        # Blocking (closes #15): root cause found via CI diagnostic capture —
        # the Open panel's confirm Return could race the Go-to-Folder sheet's
        # dismissal and get swallowed, leaving open-panel open indefinitely.
        # Fixed by retrying the confirm keystroke until the panel actually
        # closes (EditorSmokeTests.swift). Verified 3/3 clean runs on this
        # runner after the fix (run 29715663140, attempts 1-3).
        run: >
          xcodebuild test -project Meridian.xcodeproj -scheme Meridian
          -destination 'platform=macOS' CODE_SIGN_IDENTITY="-"
          CODE_SIGNING_REQUIRED=YES CODE_SIGNING_ALLOWED=YES
      - name: App performance budget tests
        run: >
          xcodebuild test -project Meridian.xcodeproj -scheme MeridianPerformanceTests
          -destination 'platform=macOS' CODE_SIGN_IDENTITY="-"
          CODE_SIGNING_REQUIRED=YES CODE_SIGNING_ALLOWED=YES


  perf-smoke:
    runs-on: macos-15
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - name: Cache SPM
        uses: actions/cache@v4
        with:
          path: Packages/DocumentCore/.build
          key: spm-${{ runner.os }}-DocumentCore-release-${{ hashFiles('Packages/DocumentCore/Package.swift', 'Packages/DocumentCore/Package.resolved') }}
          restore-keys: |
            spm-${{ runner.os }}-DocumentCore-release-
      - name: Release perf smoke (RopeBenchmarks, default scale)
        run: swift test -c release --package-path Packages/DocumentCore --filter RopeBenchmarks
