# Project Guidelines & Rules

## Swift Code Formatting & Linting
- **Mandatory Lint Check Before Commit**: Always run `swiftformat --lint .` (or `swiftformat .`) before committing any Swift code edits to ensure full compliance with the repository's `.swiftformat` rules and prevent CI lint job failures.
